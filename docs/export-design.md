# DLL 导出层设计

## 1. 目的

本文定义几何库未来生成 `dll + import lib + headers` 时的导出规则。

## 2. 设计重点

- 导出宏统一管理
- 限制 ABI 直接暴露复杂内部对象
- 明确哪些类型适合头文件内联，哪些类型适合通过服务层暴露

## 3. 当前固定结论

- 优先暴露稳定值类型与稳定函数签名
- 复杂对象尽量减少 ABI 承诺
- 导出规则与 SDK 设计保持一致
