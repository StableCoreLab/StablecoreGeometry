# 命名收口待办

本文档只保留当前仍可继续收口的事项。

## 已完成方向

- 仓库、工程和 target 继续保留 `SCGeometry`
- 公开命名空间目标收敛为 `Geometry`
- 公开头按 `Core`、`Geometry2d`、`Geometry3d`、`Brep`、`Support`、`Types`、`Serialize`、`Export` 分层
- 默认安装面不再包含兼容头和细节头

## 仍可继续检查的事项

- `Geometry::Sdk` 临时别名何时完全移除
- 是否还有残留的旧兼容命名出现在公开头或测试提示中
- 文档中是否还有未转为当前发布口径的表述

## 检查顺序

1. 先检查公开头
2. 再检查测试
3. 再检查文档
4. 最后检查安装集合

## 结束标准

- 公开 API 不再需要任何旧兼容命名
- 文档不再出现历史过程叙述
- 测试与代码都只引用当前发布面
